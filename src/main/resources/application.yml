spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microserviciousuario?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate  # O "validate" si quieres que solo valide sin modificar
    show-sql: true
    

