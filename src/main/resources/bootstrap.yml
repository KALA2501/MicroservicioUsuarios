spring:
  application:
    name: usuarios-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      name: usuarios-service

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka
